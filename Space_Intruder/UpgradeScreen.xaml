<Window x:Class="Space_Intruder.UpgradeScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybierz Ulepszenia" 
        Width="450" Height="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3A3A5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                BorderThickness="2"
                                BorderBrush="#6A6A8A">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A4A7A"/>
                                <Setter Property="Foreground" Value="#FFD700"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A2A4A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#1A1A2A"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </Window.Resources>

    <Border Background="#FF0A0A2A" CornerRadius="15" BorderThickness="2" BorderBrush="#6A6A8A">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title with glow effect -->
            <TextBlock Text="WYBIERZ ULEPSZENIA" 
                       Grid.Row="0"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#FFD700"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,15">
                <TextBlock.Effect>
                    <DropShadowEffect ShadowDepth="0" Color="#FFD700" BlurRadius="10" Opacity="0.7"/>
                </TextBlock.Effect>
            </TextBlock>

            <!-- Upgrade points indicator -->
            <StackPanel x:Name="PointsPanel" 
                        Grid.Row="1"
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,0,0,10">
                <StackPanel.Resources>
                    <Style TargetType="Ellipse">
                        <Setter Property="Width" Value="20"/>
                        <Setter Property="Height" Value="20"/>
                        <Setter Property="Margin" Value="5,0"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect ShadowDepth="0" Color="Gold" BlurRadius="5"/>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </StackPanel.Resources>
            </StackPanel>

            <TextBlock x:Name="PointsText" 
                       Grid.Row="2"
                       Text="Pozostało ulepszeń: 3/3" 
                       Foreground="#AAAAFF"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- Upgrade buttons -->
            <StackPanel Grid.Row="3" Margin="30,0">
                <Button x:Name="DamageUpgradeBtn" Content="+2 Obrażenia" 
                        Click="DamageUpgrade_Click"
                        ToolTip="Zwiększa obrażenia pocisków">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="0" Color="#FF5555" BlurRadius="10"/>
                    </Button.Effect>
                </Button>

                <Button x:Name="AttackSpeedUpgradeBtn" Content="+0.5 Szybkostrzelność" 
                        Click="AttackSpeedUpgrade_Click"
                        ToolTip="Zmniejsza czas między strzałami">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="0" Color="#55FF55" BlurRadius="10"/>
                    </Button.Effect>
                </Button>

                <Button x:Name="MovementUpgradeBtn" Content="+5 Prędkość ruchu" 
                        Click="MovementUpgrade_Click"
                        ToolTip="Zwiększa prędkość poruszania się">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="0" Color="#5555FF" BlurRadius="10"/>
                    </Button.Effect>
                </Button>

                <Button x:Name="HealthUpgradeBtn" Content="+1 Życie" 
                        Click="HealthUpgrade_Click"
                        ToolTip="Dodaje dodatkowy punkt życia">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="0" Color="#FF55FF" BlurRadius="10"/>
                    </Button.Effect>
                </Button>
            </StackPanel>

            <!-- Continue button -->
            <Button x:Name="ContinueBtn" 
                    Grid.Row="4"
                    Content="KONTYNUUJ" 
                    Click="Continue_Click"
                    Margin="30,20,30,30"
                    Background="#2A8A2A"
                    FontSize="18">
                <Button.Effect>
                    <DropShadowEffect ShadowDepth="0" Color="#00FF00" BlurRadius="15"/>
                </Button.Effect>
            </Button>
        </Grid>
    </Border>
</Window>